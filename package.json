{
  "name": "toppings-monorepo",
  "private": true,
  "workspaces": ["packages/constants", "web-ext", "website", "backend"],
  "scripts": {
    "check": "bun run scripts/check-consistency.ts",
    "bump:patch": "bun run scripts/bump-version.ts patch",
    "bump:minor": "bun run scripts/bump-version.ts minor --snapshot",
    "bump:major": "bun run scripts/bump-version.ts major --snapshot"
  }
}
