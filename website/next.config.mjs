/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/toppings",
  images: {
    unoptimized: true,
  },
  output: "export",
  reactStrictMode: true,
};

export default nextConfig;
